# Trained model artifacts (tf_classifier.h5, tokenizer.pickle) are generated
# by `python -m src.ml.train_classifier` and saved into this directory.
# They are intentionally excluded from version control (see .gitignore).
